<!-- GENERATED by scripts/gen-agent-files.mjs from Farvist 1.8.0's compiled CSS — do not edit. -->

# Theming, skins and tokens

Dark by default. Add data-theme="light" to <html> to switch at runtime. RE-BRANDING (v1.3): every derived color (gradients, glows, meshes, body orbs, hover/active states, soft tints, focus rings) derives from the --fv-* custom properties at runtime — override --fv-primary / --fv-accent / --fv-info (and optionally --fv-success/danger/warning/secondary) on :root and the whole framework recolors, no build step. Companions: --fv-primary-text (readable tint for text on the dark surface) and --fv-{color}-contrast (text on filled components) when a brand flips between light and dark fills. NEVER re-color by overriding component CSS. SKINS (v1.4): prebuilt AA-gated theme packs on the same attribute — data-theme="synthwave|cyber|noir|forest|dawn" (dawn is light). JS: Farvist.theme("synthwave") applies + persists; <button data-fv-theme-cycle="synthwave,cyber"> cycles.

## Skins

<html data-theme="NAME"> or Farvist.theme("NAME") — prebuilt AA-gated theme packs; "dawn" is a light skin. Default (no attribute) is the dark glass theme.

Names: `cyber`, `dawn`, `forest`, `noir`, `synthwave`

## Tokens (defaults)

Every design token, with its default. Override any of them on :root (or a [data-theme] block) and the whole framework follows at runtime — gradients, glows, meshes, hover states included. No build step. The optional tokens have no :root default — they exist purely as override hooks (each falls back to a sensible compiled value).

```css
:root {
  --fv-primary: #6d4af5;
  --fv-secondary: #7c8aa5;
  --fv-success: #34d399;
  --fv-danger: #fb5b78;
  --fv-warning: #fbbf24;
  --fv-info: #22d3ee;
  --fv-accent: #e879f9;
  --fv-light: #e7ecf5;
  --fv-dark: #0b0f1d;
  --fv-gray-100: #f1f5f9;
  --fv-gray-200: #e2e8f0;
  --fv-gray-300: #cbd5e1;
  --fv-gray-400: #94a3b8;
  --fv-gray-500: #64748b;
  --fv-gray-600: #475569;
  --fv-gray-700: #334155;
  --fv-gray-800: #1e293b;
  --fv-gray-900: #0f172a;
  --fv-white: #ffffff;
  --fv-black: #000000;
  --fv-body-bg: #060912;
  --fv-bg-gradient: radial-gradient(48rem 48rem at 82% -12%, color-mix(in srgb, var(--fv-primary) 28%, transparent), transparent 60%), radial-gradient(36rem 36rem at -8% 18%, color-mix(in srgb, var(--fv-info) 20%, transparent), transparent 58%), radial-gradient(40rem 40rem at 50% 118%, color-mix(in srgb, var(--fv-accent) 18%, transparent), transparent 58%);
  --fv-body-color: #e6ebf5;
  --fv-muted: #95a3c4;
  --fv-primary-text: rgb(58.7764705882%, 48.8941176471%, 97.1764705882%);
  --fv-border-color: rgba(255, 255, 255, 0.1);
  --fv-code-bg: rgba(255, 255, 255, 0.07);
  --fv-code-color: var(--fv-info);
  --fv-bg-pattern: rgba(255, 255, 255, 0.10);
  --fv-glass-bg: rgba(255, 255, 255, 0.06);
  --fv-glass-bg-strong: rgba(255, 255, 255, 0.1);
  --fv-glass-border: rgba(255, 255, 255, 0.14);
  --fv-glass-blur: 16px;
  --fv-glass-saturate: 140%;
  --fv-surface-solid: #121a2e;
  --fv-font-base: Inter, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif, Apple Color Emoji, Segoe UI Emoji;
  --fv-font-mono: ui-monospace, SFMono-Regular, SF Mono, JetBrains Mono, Menlo, Consolas, Liberation Mono, monospace;
  --fv-line-height: 1.6;
  --fv-border-radius: 0.75rem;
  --fv-border-width: 1px;
  --fv-transition: 0.2s ease;
  --fv-focus-ring: 0 0 0 3px color-mix(in srgb, var(--fv-primary) 45%, transparent);
  --fv-shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
  --fv-shadow-md: 0 8px 20px rgba(0, 0, 0, 0.35);
  --fv-shadow-lg: 0 18px 44px rgba(0, 0, 0, 0.45);
  --fv-shadow-xl: 0 30px 70px rgba(0, 0, 0, 0.55);
  --fv-glow-primary: 0 0 26px color-mix(in srgb, var(--fv-primary) 55%, transparent);
  --fv-glow-accent: 0 0 26px color-mix(in srgb, var(--fv-accent) 55%, transparent);
  --fv-glow-info: 0 0 26px color-mix(in srgb, var(--fv-info) 55%, transparent);
  --fv-glow-success: 0 0 26px color-mix(in srgb, var(--fv-success) 50%, transparent);
  --fv-glow-danger: 0 0 26px color-mix(in srgb, var(--fv-danger) 50%, transparent);
  --fv-glow-warning: 0 0 26px color-mix(in srgb, var(--fv-warning) 50%, transparent);
  --fv-gradient-primary: linear-gradient(135deg, var(--fv-primary) 0%, var(--fv-info) 100%);
  --fv-gradient-accent: linear-gradient(135deg, var(--fv-accent) 0%, var(--fv-primary) 100%);
  --fv-gradient-sunset: linear-gradient(135deg, var(--fv-danger) 0%, var(--fv-warning) 100%);
  --fv-gradient-aurora: linear-gradient(135deg, var(--fv-success) 0%, var(--fv-info) 100%);
}
```

## Optional override hooks (no default)

- `--fv-btn-color` — unset (optional — per-skin/per-context override; falls back to a compiled default)
- `--fv-btn-bg` — unset (optional — per-skin/per-context override; falls back to a compiled default)
- `--fv-btn-border` — unset (optional — per-skin/per-context override; falls back to a compiled default)
- `--fv-btn-hover-bg` — unset (optional — per-skin/per-context override; falls back to a compiled default)
- `--fv-btn-hover-border` — unset (optional — per-skin/per-context override; falls back to a compiled default)
- `--fv-btn-glow` — unset (optional — per-skin/per-context override; falls back to a compiled default)
- `--fv-btn-active-bg` — unset (optional — per-skin/per-context override; falls back to a compiled default)
- `--fv-btn-focus-ring` — unset (optional — per-skin/per-context override; falls back to a compiled default)
- `--fv-primary-contrast` — unset (optional — readable text color on that color's fills; set it when your brand flips between light and dark)
- `--fv-secondary-contrast` — unset (optional — readable text color on that color's fills; set it when your brand flips between light and dark)
- `--fv-success-contrast` — unset (optional — readable text color on that color's fills; set it when your brand flips between light and dark)
- `--fv-danger-contrast` — unset (optional — readable text color on that color's fills; set it when your brand flips between light and dark)
- `--fv-warning-contrast` — unset (optional — readable text color on that color's fills; set it when your brand flips between light and dark)
- `--fv-info-contrast` — unset (optional — readable text color on that color's fills; set it when your brand flips between light and dark)
- `--fv-accent-contrast` — unset (optional — readable text color on that color's fills; set it when your brand flips between light and dark)
- `--fv-light-contrast` — unset (optional — readable text color on that color's fills; set it when your brand flips between light and dark)
- `--fv-dark-contrast` — unset (optional — readable text color on that color's fills; set it when your brand flips between light and dark)
- `--fv-secondary-text` — unset (optional — readable text tint of that color on the page surface)
- `--fv-success-text` — unset (optional — readable text tint of that color on the page surface)
- `--fv-danger-text` — unset (optional — readable text tint of that color on the page surface)
- `--fv-warning-text` — unset (optional — readable text tint of that color on the page surface)
- `--fv-info-text` — unset (optional — readable text tint of that color on the page surface)
- `--fv-accent-text` — unset (optional — readable text tint of that color on the page surface)
- `--fv-light-text` — unset (optional — readable text tint of that color on the page surface)
- `--fv-dark-text` — unset (optional — readable text tint of that color on the page surface)
- `--fv-primary-tint-text` — unset (optional — readable text tint of that color on the page surface)
- `--fv-status` — unset (optional — per-skin/per-context override; falls back to a compiled default)
